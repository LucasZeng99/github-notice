import React from "react";

const Form = props => {
  return (
    <div className="form">
      <input type="text" placeholder="Enter name" />
      <button>Submit</button>
    </div>
  );
};

export default Form;
